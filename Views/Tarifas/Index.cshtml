@model IEnumerable<AParCarWeb.Models.Tarifa>

@{
    ViewData["Title"] = "Tarifas";
}

<div style="height:30px;"></div>

<div class="d-flex justify-content-between align-items-center mb-3">
    <h1 class="mb-0">
        <i class="bi bi-cash-coin me-2"></i>Tarifas
    </h1>
</div>

<div class="card shadow-sm">
    <div class="table-responsive">
        <table class="table table-hover align-middle mb-0">
            <thead class="table-light">
                <tr>
                    <th>@Html.DisplayNameFor(model => model.Descripcion)</th>
                    <th>@Html.DisplayNameFor(model => model.PrecioHora)</th>
                    <th class="text-end">Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td><strong>@item.Descripcion</strong></td>
                        <td>₡ @item.PrecioHora</td>
                        <td class="text-end">
                            <a asp-action="Edit" asp-route-id="@item.TarifaId"
                                   class="btn btn-sm btn-outline-primary">
                                <i class="bi bi-pencil-square"></i>
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>